print styles

Try some different photoshop stuff on the oak leaves. it's a bit ugly

To fix flicker bug on classes hover, put the hover selector on a parent element

Title SVG alt text? Or figure out how to handle fonts

*https://fullcalendar.io/

*use grid, but include fallback to float css
	Bootstrap 4 uses grid, but does it have backwards compatibility built in?

*take a look at google analytics

*svg fallbacks (https://css-tricks.com/a-complete-guide-to-svg-fallbacks/)

*remove unused font weights.

*should I change unique classes to ids

DESIGN:

*map-markers: try blue, inner fill on all, shadow

*change navbar color so it's different from other backgrounds

text sharpen? anti-alias?

different bottom pic?